name: Deploy React (Vite) to BT

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v3

      - name: 🔧 Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: 📦 Install & Build
        run: |
          npm install
          npm run build

      # ⭐ 关键：确认 dist 一定存在
      - name: 🔍 Verify dist
        run: |
          echo "检查 dist 是否存在"
          if [ ! -d "dist" ]; then
            echo "❌ dist 目录不存在"
            exit 1
          fi
          echo "dist 内容："
          ls -lh dist
          echo "dist 文件数量："
          find dist | wc -l

      - name: 📤 Upload dist
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.KEY }}
          source: "dist/**"
          target: "/tmp/react-deploy/"
          strip_components: 1

      - name: 🚀 Deploy to BT
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.KEY }}
          script: |
            TARGET=/www/wwwroot/redcowx.com

            echo "🧹 清空旧文件"
            rm -rf ${TARGET:?}/*

            echo "📦 拷贝新文件"
            cp -r /tmp/react-deploy/* $TARGET/

            echo "🧹 清理临时目录"
            rm -rf /tmp/react-deploy

            echo "✅ 部署完成"
